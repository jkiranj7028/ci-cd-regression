[pytest]
addopts = -q --maxfail=1
testpaths = tests
markers =
    smoke: critical path, fastest checks
    regression: regression suite for previously fixed bugs
    integration: integration/API tests
